// Copyright 2025 bhipp
// test functions written in somefunctions.h / .cc on Thursday
#include<iostream>
using std::cout;
using std::endl;
using std::cin;
#include"somefunctions.h"

int main() {
  TowerSolution(8);
/*  int x, y;
  cout << "Enter two integers ";
  cin >> x >> y;
  cout << "The GCD of " << x << " and " << y << " is " << GCD(x, y) << endl;
*/
/*
  for ( int i = 0; i < 44; ++i ) 
    cout << Fibonacci(i) << endl;
*/
/*  for ( int i = 0; i < 10; ++i )
    cout << i << "! = " << Factorial(i) << " = "
         << FactorialI(i) << endl;
*/
  return 0;
}
